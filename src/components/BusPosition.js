import React from 'react';

export default class BusPosition extends React.Component {
    render(props) {
        <div style={{
            position: 'relative', color: 'white', background: 'red',
            height: 40, width: 60, top: -20, left: -30,    
            }}>
        </div>
    }
}